import { ReactElement } from "react";
import { LineColumnContentProps } from "../types";
/**
 * Generates background-image CSS for "barber pole" effect
 * @param routeColor  the background color. Assumed to be hex.
 */
export declare const barberPole: (routeColor: string, gap?: number) => string;
export default function LineColumnContent({ interline, isDestination, lastLeg, leg, legIndex }: LineColumnContentProps): ReactElement;
//# sourceMappingURL=line-column-content.d.ts.map