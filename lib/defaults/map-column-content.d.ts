import { ReactElement } from "react";
import { MapColumnContentProps } from "../types";
/**
 * Default component for rendering the map button column content.
 * Renders the standard map icon button that triggers the frameLeg action.
 */
export default function MapColumnContent({ onClick }: MapColumnContentProps): ReactElement;
//# sourceMappingURL=map-column-content.d.ts.map