import { ReactElement } from "react";
interface Props {
    color?: string;
    abbreviation?: string;
    name: string;
}
export default function RouteBadge({ color, abbreviation, name }: Props): ReactElement;
export {};
//# sourceMappingURL=index.d.ts.map